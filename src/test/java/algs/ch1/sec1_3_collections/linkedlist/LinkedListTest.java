package algs.ch1.sec1_3_collections.linkedlist;


public class LinkedListTest {
  // eu sei, eu sei. Não é a melhor estratégia pra teste.
  public void testRemoveNode() {
    // int n = 10;
    // LinkedList<Integer> list = new LinkedList<>();

    // for (int i = 0; i < n; i++) {
    //   list.addLast(i);
    // }

    // for (int i = n-1; i >= 0; i--) {
    //   assertEquals(i, list.removeAt(i));
    // }

    // n = 100;
    // for (int i = 0; i < n; i++) {
    //   list.addLast(i);
    // }

    // for (int i = n-1; i >= 0; i--) {
    //   assertEquals(i, list.removeAt(i));
    // }
  }
}